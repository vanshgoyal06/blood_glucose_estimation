# Models

`catboost_250_legacy_random_split.cbm` is the packaged primary CatBoost model trained using the legacy row-level validation design. It is retained for reproducibility, not as evidence of independently validated clinical performance.

The clean participant-level audit model is not treated as the official production model.
